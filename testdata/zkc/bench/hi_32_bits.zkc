pub input data(address:u1)->(data:u64)
pub output hi(address:u1)->(hi:u32)

fn main() {
  hi[0] = hi_32_bits(data[0])
  return
}

fn hi_32_bits(value:u64) -> (hi:u32) {
   hi = (value >> 32) as u32
   return
}
