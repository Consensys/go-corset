include "../../impl/felt/poseidon.zkc"
include "../../impl/felt/state.zkc"
include "../../utils/memory.zkc"

// ===================================================================
// read_from_state test
// ===================================================================

fn main<ram, felt_state>() {
    // we write the message 0x00 01 02 ... fe ff into memory
    var zero:u32 = read_from_state(0)
    write_to_state(1, 1)
    var one:u32 = read_from_state(1)

    if zero != 0 {
        fail
    }
    if one != 1 {
        fail
    }
}
